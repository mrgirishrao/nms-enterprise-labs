eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      instance:
    # This makes the ID completely unique by attaching a random string to each process
    instance-id: ${spring.application.name}:${random.value}